/* =====================================================================
   school-config.example.js — 학교 설정 파일 "견본"
   ---------------------------------------------------------------------
   ⚠ 이 파일은 참고용 예시입니다. 실제로는 직접 쓰지 마시고,
     사이트의 ⚙ 관리자 설정(#setup)에서 자동 생성되는
     school-config.js 를 업로드하세요. (파일명이 정확히 school-config.js 여야 적용됩니다)
   아래는 가상의 "한빛고등학교" 예시로, 각 항목의 의미를 보여줍니다.
===================================================================== */
window.SCHOOL_CONFIG = {
  /* ── 학교 표기 ── */
  name: "한빛고등학교",                       // 학교 이름 (헤더·체크리스트 등 전체 반영)
  mark: "한",                                 // 로고 원 안의 한 글자 (생략 시 이름 첫 글자)
  subtitle: "2027 교육과정 박람회",           // 박람회 이름
  badge: "경기 수원시 · 한빛고등학교",        // 첫 화면 지역 배지
  ckSubtitle: "2027학년도 입학생 · 학기당 29학점",
  planTitle: "한빛고등학교 2027학년도 3개년 교육과정 편성표",
  footDesc: "경기도 수원시",

  /* ── 결과 수집 (둘 중 하나) ── */
  submitUrl: "",                              // 방법 B: 내 앱스크립트 /exec URL
  sheetId: "",                                // 방법 A: 내 구글 시트 ID (URL의 /d/와 /edit 사이)

  /* ── 길라잡이 영상 ── */
  videos: [
    { subj: "국어", desc: "국어 교과 선택과목 길라잡이", vid: "5-WoCOfQ2Gc", type: "general" },
    { subj: "수학", desc: "수학 교과 선택과목 길라잡이", vid: "ZAoDv229u6M", type: "general" }
    // vid = 유튜브 주소의 11자리 영상 ID
  ],
  channelUrl: "https://www.youtube.com/@진로학업설계컨설팅",

  /* ── 편성표·체크리스트 ──
     #setup에서 편성표 엑셀을 올리면 아래 두 항목(plan, checklist)이
     자동 생성되어 들어갑니다. 수백 줄 데이터라 예시에서는 생략합니다. */
  // plan: { gradeLabels:[…], rows:[…], totals:{…} },
  // checklist: [ {name:"1학년", semesters:[…]}, … ],
  // totalCredits: 174,
  jakjeonRules: false   // 작전고 전용 선택 규칙(계열 고정·연계) 사용 안 함
};
